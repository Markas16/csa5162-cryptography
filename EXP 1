#include <stdio.h>

int main() {
    char text[100];
    int key,i;

    printf("Enter Text: ");
    scanf("%s", text);

    printf("Enter Key: ");
    scanf("%d",&key);

    for(i=0;text[i]!='\0';i++)
        text[i]=((text[i]-'A'+key)%26)+'A';

    printf("Cipher Text: %s",text);
    return 0;
}
